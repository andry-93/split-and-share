# 💸 Split & Share

**Split & Share** — мобильное приложение для учёта совместных расходов, участников и долгов между ними.  
Подходит для путешествий, вечеринок, совместной аренды и любых групповых расходов.

---

## ✨ Основные возможности

### 📅 События (Events)
- Создание, редактирование и удаление событий
- Валюта события (или валюта по умолчанию из настроек)
- Просмотр участников, расходов и долгов

### 👥 Участники (Participants)
- Компактный список без карточек
- Группировка A–Z
- Аватары (image picker)
- Выделение и массовое удаление
- Полная синхронизация с Events и Expenses

### 💳 Расходы (Expenses)
- Учёт: кто заплатил и сколько
- Валюта события
- Диалог добавления / редактирования
- Скроллируемый выбор плательщика
- Корректное вертикальное выравнивание суммы

### 🔄 Долги (Debts)
- Автоматический пересчёт в реальном времени
- Анимация при изменениях
- Цветовая индикация (кто должен / кому должны)
- Детальный просмотр долга
- Экспорт долгов

### ⚙️ Настройки (Settings)
- Язык (через `react-i18next`)
- Тема (light / dark)
- Валюта по умолчанию

---

## 🧱 Архитектура проекта

Проект построен с упором на **чистую архитектуру и масштабируемость**:

src/

├── app/ # навигация, провайдеры

├── entities/ # бизнес-сущности (Event, Expense, Debt)

├── features/ # экраны и бизнес-логика

├── shared/ # UI, hooks, utils

└── i18n/ # локализация


### Основные принципы
- Hooks-first архитектура
- Изоляция бизнес-логики
- AsyncStorage как persistence layer
- Минимум зависимостей
- Предсказуемый state-flow

---

## 🪟 BottomSheet (кастомная реализация)

В проекте реализован собственный BottomSheet без сторонних библиотек:

### Возможности
- Snap points: `small | medium | large | auto`
- `auto` — высота по контенту
- Scroll-aware (PanResponder + ScrollView)
- Blur backdrop (`expo-blur`)
- Swipe down to close
- Блокировка повторного открытия
- Корректное уничтожение после закрытия

### Использование

```tsx
const { show } = useBottomSheet();

show(
  <DebtDetailsContent debt={debt} />,
  { snap: 'auto' }
);
```

---

## 🌍 Локализация (i18n)
### Используется react-i18next:
- Live-переключение языка
- Интерполяция значений
- Единый useTranslation()

```tsx
t('amount_hint', { currency })
```

---

## 💰 Форматирование валют
### Используется Intl.NumberFormat:
- Формат: 12.50 BYN
- Без символа валюты
- Locale берётся из Settings
- Общий хук useCurrencyFormatter

## 🚀 Запуск проекта
```bash
npm install
npx expo start
```
или
```bash
yarn
yarn expo start
```

---

## 🛠 Стек технологий
- React Native
- Expo
- react-native-paper
- react-navigation
- react-i18next
- AsyncStorage
- expo-blur

---

## 📌 Статус проекта
- ✅ MVP готов
- ✅ Архитектура стабильна
- 🚧 Возможны UX-улучшения
- 🚧 Возможна cloud-синхронизация
